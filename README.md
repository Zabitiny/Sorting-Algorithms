# Sorting-Algorithms
Program will prompt user for a file to read in &amp; write out to. Then it will ask for which sorting algorithm to use, and show how the sorting algorithm affects each line of values from the file read in. The file read in should only have integer values, and the sorted sets will be displayed in the specified write out file.
